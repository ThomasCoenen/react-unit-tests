export const formatCity = () => {

}

export const multiply = (val1, val2) => {
    return val1 * val2
}

export const makeLowerCase = (string) => {
    return string.toLowerCase()
}

// let address
// let city = address.split(',').reverse()[2].split(" ")
// let state = address.split(',').reverse()[1].replace(/\s+/, "") //removes whitespace

// if (city[0] === "") {
//     city.shift() //remove blank space from beginning 
// }
// //console.log(city)
// let city2
// if (city.length > 1) {
//     city2 = city.join(" ")
// } else {
//     city2 = city.join("")
// }
// console.log(city2)
// console.log(state)